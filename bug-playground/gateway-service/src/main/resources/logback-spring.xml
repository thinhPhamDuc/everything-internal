<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} [correlation_id=%X{correlation_id}] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Gửi thẳng JSON qua TCP tới Logstash (input tcp/5000, codec json_lines)
         - không cần Filebeat/Promtail vì mỗi service chỉ có 1 log stream nhỏ,
         phù hợp quy mô playground. correlation_id trong MDC tự động được
         đính kèm nhờ includeMdc. -->
    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>${LOGSTASH_HOST:-logstash}:${LOGSTASH_PORT:-5000}</destination>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdc>true</includeMdc>
            <customFields>{"service":"gateway-service"}</customFields>
        </encoder>
        <keepAliveDuration>5 seconds</keepAliveDuration>
        <reconnectionDelay>1 second</reconnectionDelay>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="LOGSTASH"/>
    </root>
</configuration>
